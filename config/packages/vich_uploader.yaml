vich_uploader:
    db_driver: orm
    mappings:
        layer_files:
            uri_prefix: /api/layers/file
            upload_destination: '%kernel.project_dir%/var/private/layers'
            namer:
                service: Vich\UploaderBundle\Naming\SmartUniqueNamer
                options: { keep_extension: true }
