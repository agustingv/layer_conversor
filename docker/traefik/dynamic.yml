tls:
  stores:
    default:
      defaultCertificate:
        certFile: /certs/cert.pem
        keyFile:  /certs/key.pem

http:
  serversTransports:
    # Used for the nginx backend — skips verification of the self-signed cert
    skipVerify:
      insecureSkipVerify: true
